<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.management.timemanagement">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.answers.QAActivity"></activity>
        <activity android:name=".ui.authorization.RegisterActivity" />
        <activity android:name=".ui.authorization.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.project.ProjectTaskActivity" />
        <activity android:name=".ui.task_add.EditTaskActivity" />
        <activity android:name=".ui.project_add.EditProjectActivity" />
        <activity android:name=".ui.task_ready.ReadyTaskActivity" />
        <activity android:name=".ui.task_add.AddActivity" />
        <activity android:name=".ui.project_add.AddProjectActivity" />
        <activity android:name=".ui.project.ProjectTaskAddActivity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>